%h2
  User:
  = @user.username
%p 
  Member since
  = @user.created_at.to_s(:long)
%p
  = @user.profile
%h3 Photos
%ul#photos
  = render :partial => 'photos/photo', :collection => @photos
%p
  = link_to "See all of #{@user.username}'s photos",user_photos_path(:user_id => @user)