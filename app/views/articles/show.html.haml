%h2=@article.title
- if @article.category
  %p.category
    Category:
    = link_to @article.category.name,category_articles_path(@article.category)
  %p
    = @article.created_at.to_s(:short)
    = textilize(@article.body)
%p= link_to 'Back to article list', articles_path